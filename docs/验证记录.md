# 验证记录

## 1.5.1 原处编辑与提示

- 方案名称支持双击重命名；输入码和优先级支持双击原处修改。回车、失焦保存，Esc 取消，F2 提供键盘入口。中文输入法确认候选不会提前保存。
- 空名称、非法输入码、重复的输入码与词条、超出 1–99 的优先级会在对应位置提示；保存失败保留填写内容。
- Chromium 断网打开本地 HTML，核对名称与字段修改、候选顺序同步、搜索中的词条移出、切换方案、方案排序、刷新及 JSON 备份；原词条元数据和关联标记保留。
- 检查实际下载的搜狗文件，确认修改后的输入码与优先级进入导出、停用词条未导出；多窗口冲突拒绝覆写并保留草稿。
- 四个表头的鼠标悬停、移开隐藏、键盘聚焦和 Esc 关闭均通过；检查日间、夜间及 390px、320px 窄屏，未出现页面整体横向溢出。
- 类型检查、既有六种导出格式及存储、关联、部件筛选测试通过。浏览器检查没有页面脚本异常或 HTTP(S) 请求。预置词库和来源署名未改动。
- 此次浏览器验证使用 Linux Chromium，未在真实 macOS Safari 或 Windows 输入法中重新实机导入。

## 1.1.0 验证记录

## 已完成

- TypeScript 类型检查。
- 七个预置方案通过数据校验，数量与数量说明一致。
- 微软原始 DAT 的 383 条记录可按原顺序、元数据逐字节还原。
- 六种输入法导出包的格式、备份、同码排名、排除规则及手机搜狗分包测试。
- IndexedDB 新建、保存、关闭重开、旧版本拒绝、两个实例竞争写入、JSON 恢复、存储不可用及损坏记录保护测试。
- 在 Linux Chromium 实际打开 `file://` HTML，并将浏览器设为断网：新增、刷新后保留、JSON 下载、微软 ZIP 下载、删除、JSON 恢复均完成。
- 对浏览器实际下载的 DAT 重新解析，确认包含此次新增的词条。
- 关闭浏览器后使用同一测试配置重新打开，保存记录仍可读取。
- 1440 像素桌面和 390 像素窄屏中文界面核对，窄屏没有页面整体横向溢出。
- 检查期间页面没有 JavaScript 异常或 HTTP(S) 网络请求；测试浏览器保留正常网页安全校验。
- 发行文件不包含测试用浏览器配置、私人在线词库、原网站登录和数据库配置。

## 验证范围

微软拼音的真实输入法导入成功由使用者反馈，Windows 和输入法具体版本未记录。
离线网页测试使用 Linux Chromium；未逐一实测所有 Windows、macOS、iOS、Android 浏览器或其他输入法客户端。生僻字是否显示还取决于使用者设备上的字体。

本地存储不可用时退回临时会话并明确提示；跨浏览器、文件位置变化和清理浏览数据后的恢复，应以下载的 JSON 备份为准。

## 1.1.1 名称与文档更新

项目包名与下载包前缀统一为 `ime-dictionary-exporter`，英文全称为 Multi-platform IME Custom Dictionary Exporter。上述浏览器交互记录来自此前功能版本，本次没有改动界面交互及导出逻辑。内部数据库标识沿用旧值，兼容原有保存数据。

## 1.2.0 清理与内嵌指南

- 清除事务删除全部词库内容，只留下不含词条的版本标记；刷新不会自动重建。
- 验证旧窗口写入被拒绝、清除失败事务回滚、其他存储位置保留、显式重新启用默认方案和 JSON 恢复。
- 在 Linux Chromium 以 file:// 断网验证指南正文、七行数量表、完整 MIT 与第三方许可、清除前备份与取消、清除确认、共享窗口提示、刷新及恢复。
- 模拟网站不同路径，验证清除范围隔离；验证临时会话的清空与重新开启。
- 检查 1440 像素桌面与 390 像素手机宽度，许可和说明文字均内嵌，不请求外部文件。

## 1.3.0 设置与显示模式

基于 GitHub `main` 提交 `a585bac` 制作，保留最新 README 署名与词库来源说明，并将使用说明中的旧署名同步为「天妅」。

- 类型检查与既有导出、存储测试通过；原始微软 DAT 逐字节一致，七个预置方案未改动。
- Chromium 153 实际浏览器验证：断网打开本地 HTML，三种显示模式正常；手动选择优先于系统，跟随系统能响应实时变化，刷新保持选择。
- 用键盘方向键切换显示模式；白天与夜晚下的设置、编辑、导出、下拉列表、指南及确认窗口均已检查。设置弹窗在 320px、390px 窄屏可操作，无页面横向溢出。
- 切换显示模式不改动词库内容或保存版本；夜晚模式下仍可编辑、导出和恢复 JSON。
- 从设置中下载备份、取消清除、确认清除、刷新、重新启用默认词库和恢复备份均通过；取消后焦点返回清除按钮，清除词库后显示偏好保留。
- 同一网址的多个窗口同步显示偏好；同域不同项目路径的偏好互不影响。浏览器拒绝显示偏好存储时仍能切换，页面显示无法持久保存的提示。
- 本地运行无网络请求，无页面脚本异常。系统切换通过浏览器模拟验证；尚未在真实 macOS Safari 上实机验证。

## 1.4.0 关联标记与配色

- 增加同义、近义、异体字手动关联；按同一方案内的精确词语记录关系，不因输入码不同重复标记，不自动推断跨组关系。
- 新增关系数据测试：旧备份兼容、新备份往返恢复、重复与失效标记校验、同词多码、改名与删除清理、停用与排序保留、部分导出范围、失效窗口保存拒绝、清除后恢复均通过。
- 六种输入法的原生文件在添加标记前后逐字节一致；标记仅进入 JSON 备份。原始微软 DAT 对照及既有导出、存储测试保持通过。
- Chromium 实际浏览器断网验证通过：批量及单词标记、查看、编辑类型、移除、重复拦截、有标记筛选、刷新、改名、删除最后一条同词记录、清除后从带标记备份恢复；其他方案内容未变化。
- 日间与夜间的顶栏、表头、标记标签及关联弹窗完成截图检查；390px、320px 窄屏无页面横向溢出。测试浏览器未安装部分生僻字字形，文本与导出内容仍完整保存；实际字形由使用设备字体决定。
- 新版备份格式标为 version 2，程序接受旧格式和当前格式，拒绝未知的新格式。此版本不更改原有词库存储名称及保存版本机制。
